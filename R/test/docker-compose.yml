version: '2'
services:

  model:
    image: model:0.0.16
    build:
      context: ../
      dockerfile: Dockerfile
    container_name: model
    ports:
      - 8000:8000

  mongo:
    image: mongo:4.4.5
    hostname: mongo
    container_name: mongo
    ports:
      - 27017:2